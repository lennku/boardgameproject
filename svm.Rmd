---
title: "svm"
output: html_document
---


```{r}
library(dplyr)
library(caret)
set.seed(1)

inTrain <- createDataPartition(y = game$avg_rating,
                               p=0.8)$Resample
train_set <- slice(game, inTrain) 
test_set <- slice(game, -inTrain)

control <- trainControl(method = 'cv', number = 20)
model <- train(avg_rating ~ owned + num_votes + age + max_time + max_players + weight,
             data = train_set,
             method = "svmLinear",
             na.action=na.exclude,
             trControl = control,  
             metric = "RMSE")
model
```

Support Vector Machines with Linear Kernel 

3929 samples
   6 predictor

No pre-processing
Resampling: Cross-Validated (20 fold) 
Summary of sample sizes: 3732, 3733, 3732, 3730, 3732, 3732, ... 
Resampling results:

  RMSE       Rsquared  MAE      
  0.4606422  0.323174  0.3620871

Tuning parameter 'C' was held constant at a value of 1
